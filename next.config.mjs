/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['pin.it','api.melonest.ir'], // Add the external domain here
      },
      reactStrictMode:true
};

export default nextConfig;
